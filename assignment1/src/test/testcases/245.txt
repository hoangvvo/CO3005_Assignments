
class A {
    var a: float = 1.0;
    var b: float = x + y;
    var c: float = x - y;
    var d: float = x * y;
    var e: float = x / y;
    var f: float = x > y;
    var g: bool = x >= y;
    var h: bool = x < y;
    var i: bool = x <= y;
}
    